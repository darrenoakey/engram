[tool.ruff]
line-length = 120
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["src"]
pythonpath = ["src"]
addopts = "-q -p no:cacheprovider"

[tool.pyright]
extraPaths = ["src"]
venvPath = "."
venv = ".venv"
